package net.rikarin.domain

import kotlin.reflect.KType

object EntityHelper {
    // TODO: fix this
//    public static bool IsEntity(Type type) => typeof(IEntity).IsAssignableFrom(type);

//    fun isEntity(type: KType) =
}